import products from '../data/hrt.js';

export function renderProduct(hrt){
    // const li = document.createElement('li');
    // li.classList.add(fruit.category);
    // li.title = fruit.description;

    // const h3 = document.createElement('h3');
    // h3.textContent = fruit.name;
    // li.appendChild(h3);

    // const img = document.createElement('img');
    // img.src = `../assets/${fruit.image}`;
    // img.alt = `${fruit.name} image`;
    // li.appendChild(img);

    // const p = document.createElement('p');
    // p.classList.add('price');
    // p.textContent = `$${price.toFixed(2)}; OR `$${price.toLocaleString(...)}

    // const btn = document.createElement('button');
    // button.textContent =
    // button.value = 
    // p.appendChild(btn);
    // //append btn inside p and p inside li for appropriate realtionships
    // li.appendChild(p);
};